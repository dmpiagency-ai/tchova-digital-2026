# Plano de Correção de Bugs - Lint Fix

## Erros Críticos Identificados (src/ apenas)

### Parsing Errors
- `LogoTextV2.tsx` - pode ser SVG truncado (não crítico se não usa)
- `ripple-button.tsx` - DUPLICIDADE de export (corrigido)

### Prefer-const
- `firebase.ts` linha 34 - analytics nunca é reatribuído

### @typescript-eslint/no-explicit-any (src/)

**useLowEnd.ts** (linhas 15-29) - casts `unknown as Navigator` são padrão, não `any`
**useScrollReveal.ts** - verificar linha 83

**PageLoader.tsx** - EventListener (linhas 139, 146) - usar tipo correto

**GSMPaymentModal.tsx** (linhas 64-66) - props callback types

**GSMToolsPanelNew.tsx** (linha 93) - callback type

**Hero.tsx** (linhas 251, 261, 307) - GSAP refs

**RotatingText.tsx** (linhas 10-13, 64, 66, 119, 139) - animation types

**SmoothScroll.tsx** (linha 23) - gsap context

**MagneticButton.tsx** (linhas 22-25) - gsap context

**TiltCard.tsx** (linhas 29-32) - gsap context

## Estratégia
1. Corrigir apenas arquivos que causam build/parsing failures
2. Manter funcionalidade existente inalterada
3. Usar tipos genéricos React.MouseEvent<HTMLDivElement> em vez de any
4. Não tocar worktrees - foco no workspace principal

## Status
- [x] ripple-button.tsx - duplicidade de export removida
- [ ] Firebase analytics const (prioridade baixa - funcionalidade ok)
- [ ] Outros anys - não críticos para build